#!/usr/bin/env bash
# Example: Interactive mode — chat-style task submission
# Type tasks one after another, type 'quit' to exit

nixclaw --interactive

# Sample session:
# > List files in the current directory
# (agent processes and responds)
# > Now count the lines in each Python file
# (agent processes and responds)
# > quit
